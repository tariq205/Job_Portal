<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Portfolio</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Arial', sans-serif;
            background-color: #f5f5f5;
            display: flex;
            height: 100vh;
            overflow: hidden;
        }

        .sidebar {
            width: 250px;
            background-color: #2C3E50;
            color: white;
            height: 100%;
            position: fixed;
            top: 0;
            left: 0;
            padding-top: 30px;
            transition: all 0.3s ease;
            z-index: 100;
        }

        .sidebar a {
            text-decoration: none;
            color: white;
            display: block;
            padding: 15px 20px;
            font-size: 18px;
            transition: background-color 0.3s;
        }

        .sidebar a:hover {
            background-color: #1abc9c;
            transform: translateX(10px);
        }

        .sidebar .logo {
            text-align: center;
            font-size: 24px;
            margin-bottom: 40px;
            font-weight: bold;
            text-transform: uppercase;
        }

        .sidebar .toggle-btn {
            position: absolute;
            top: 20px;
            left: 100%;
            background-color: #2C3E50;
            color: white;
            border: none;
            padding: 10px;
            cursor: pointer;
            display: none;
        }

        .main-content {
            margin-left: 250px;
            padding: 20px;
            transition: margin-left 0.3s ease;
            width: 100%;
        }

        .header {
            background-color: #007bff;
            color: white;
            padding: 20px;
            text-align: center;
            border-radius: 8px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
        }

        .section {
            margin-bottom: 40px;
            padding: 30px;
            background-color: white;
            border-radius: 8px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
        }

        .section h2 {
            margin-bottom: 20px;
            font-size: 24px;
            color: #333;
        }

        .card {
            padding: 20px;
            background-color: white;
            border-radius: 8px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
            margin: 20px 0;
        }

        button {
            background-color: #007bff;
            color: white;
            padding: 12px 20px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-size: 16px;
            transition: transform 0.2s ease, background-color 0.2s;
        }

        button:hover {
            background-color: #0056b3;
            transform: translateY(-3px);
        }

        .footer {
            background-color: #007bff;
            color: white;
            padding: 10px;
            text-align: center;
            position: absolute;
            bottom: 0;
            width: 100%;
        }

        .skills, .education, .experience {
            display: flex;
            flex-wrap: wrap;
            gap: 20px;
        }

        .skill, .education-item, .experience-item {
            flex: 1 1 calc(33.33% - 20px);
            padding: 20px;
            background-color: #ecf0f1;
            border-radius: 8px;
            text-align: center;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .skill:hover, .education-item:hover, .experience-item:hover {
            transform: scale(1.05);
            box-shadow: 0 6px 20px rgba(0, 0, 0, 0.2);
        }

        @media screen and (max-width: 768px) {
            .sidebar {
                width: 200px;
                padding-top: 20px;
            }
            .sidebar a {
                font-size: 16px;
            }
            .main-content {
                margin-left: 0;
            }
            .sidebar .toggle-btn {
                display: block;
            }
        }
    </style>
</head>
<body>

    <div class="sidebar">
        <div class="logo">My Portfolio</div>
        <a href="#skills"><i class="fas fa-cogs"></i> Skills</a>
        <a href="#education"><i class="fas fa-graduation-cap"></i> Education</a>
        <a href="#experience"><i class="fas fa-briefcase"></i> Experience</a>
        <a href="#contact"><i class="fas fa-comment-alt"></i> Contact</a>
        <a href="/logout"><i class="fas fa-sign-out-alt"></i> Logout</a>
        <button class="toggle-btn" onclick="toggleSidebar()">☰</button>
    </div>

    <div class="main-content">
        <div class="header">
            <h1>Welcome to My Portfolio</h1>
        </div>
        <div class="container">
            <div class="card">
                <h3>Hello, <%= username %>!</h3>
                <p>You have successfully logged in.</p>
                <button onclick="window.location.href='/logout'">Logout</button>
            </div>

            <!-- Skills Section -->
            <div id="skills" class="section">
                <h2>Skills</h2>
                <div class="skills">
                    <div class="skill">
                        <h3>Backend Development</h3>
                        <p>Node.js, Express, MongoDB, SQL</p>
                    </div>
                    <div class="skill">
                        <h3>API Development</h3>
                        <p>REST, SOAP, GraphQL</p>
                    </div>
                    <div class="skill">
                        <h3>Version Control</h3>
                        <p>Git, GitHub</p>
                    </div>
                </div>
            </div>

            <!-- Education Section -->
            <div id="education" class="section">
                <h2>Education</h2>
                <div class="education">
                    <div class="education-item">
                        <h3>Bachelor's in Computer Science</h3>
                        <p>Dawood University of Engineering and Technology</p>
                    </div>
                </div>
            </div>

            <!-- Experience Section -->
            <div id="experience" class="section">
                <h2>Experience</h2>
                <div class="experience">
                    <div class="experience-item">
                        <h3>Intern - Systems Limited</h3>
                        <p>3 months internship at Systems Limited, Karachi</p>
                    </div>
                </div>
            </div>

            <!-- Future Goal Section -->
            <div class="section">
                <h2>Future Goal</h2>
                <p>I want to work in the future as a Backend Developer.</p>
            </div>

            <!-- Contact Section -->
            <div id="contact" class="section">
                <h2>Contact</h2>
                <p>Click below to start a chat with me on WhatsApp!</p>
                <button onclick="window.location.href='https://wa.me/923417368085'">Contact on WhatsApp</button>
            </div>
        </div>

       
    </div>

    <script>
        function toggleSidebar() {
            const sidebar = document.querySelector('.sidebar');
            const mainContent = document.querySelector('.main-content');
            sidebar.classList.toggle('active');
            if (sidebar.classList.contains('active')) {
                mainContent.style.marginLeft = '0';
            } else {
                mainContent.style.marginLeft = '250px';
            }
        }
    </script>

</body>
</html>
